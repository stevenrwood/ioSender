<UserControl x:Class="CNC.Controls.StripGCodeConfigControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CNC.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="140" d:DesignWidth="310">
    <GroupBox x:Uid="grp_gcodeStrip" Header="GCode command stripping" HorizontalAlignment="Left" VerticalAlignment="Top" Height="Auto" Width="310">
        <Grid Margin="50,0,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="70*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Label Grid.Row="0" Grid.Column="0" Content="M6:" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,2"/>
            <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding Path=CommandIgnoreStates}" SelectedItem="{Binding Path=IgnoreM6, Mode=TwoWay}" HorizontalAlignment="Left" Margin="3,2" VerticalAlignment="Center" Width="70"/>
            <Label Grid.Row="1" Grid.Column="0" Content="M7:" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,2"/>
            <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding Path=CommandIgnoreStates}" SelectedItem="{Binding Path=IgnoreM7, Mode=TwoWay}" HorizontalAlignment="Left" Margin="3,2" VerticalAlignment="Center" Width="70"/>
            <Label Grid.Row="2" Grid.Column="0" Content="M8:" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,2"/>
            <ComboBox Grid.Row="2" Grid.Column="1" ItemsSource="{Binding Path=CommandIgnoreStates}" SelectedItem="{Binding Path=IgnoreM8, Mode=TwoWay}" HorizontalAlignment="Left" Margin="3,2" VerticalAlignment="Center" Width="70"/>
            <Label Grid.Row="3" Grid.Column="0" Content="G61/G64:" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,2"/>
            <ComboBox Grid.Row="3" Grid.Column="1" ItemsSource="{Binding Path=CommandIgnoreStates}" SelectedItem="{Binding Path=IgnoreG61G64, Mode=TwoWay}" HorizontalAlignment="Left" Margin="3,2" VerticalAlignment="Center" Width="70"/>
        </Grid>
    </GroupBox>
</UserControl>
